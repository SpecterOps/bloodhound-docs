---
title: 2026-04-13 Release Notes
description: Learn about new features, enhancements, and fixed issues in BloodHound.
sidebarTitle: "2026-04-13"
---

|     |     |     |     |
| --- | --- | --- | --- |
| **Release** | **BloodHound** |  **OpenHound** | **SharpHound** | **AzureHound** |
| 2026-04-13 | v9.0.0 | v0.1.0 | v2.12.0 | v2.12.0 |

<Tip>Use the filters on the right side of this page to narrow down the updates by component. You can select multiple filters at the same time to refine your results.</Tip>

<Update label="BloodHound" description="New Feature" tags={["Administration"]}>
  {/*BED-7559*/}
  ## API Key Expiration

  Configure automatic expiration for API keys used to authenticate to the BloodHound API to reduce long-lived credentials and support internal security and compliance requirements.

  API key expiration applies to personal API tokens, non-personal API tokens used for integrations, and collector client API tokens (AzureHound, SharpHound, OpenHound).
</Update>

<Update label="OpenHound" description="New Feature" tags={["Data Collection"]}>
  {/*BED-7312*/}
  ## OpenHound Data Collector Framework

  Collect and convert data from GitHub, Jamf, and Okta with OpenHound, a standardized framework for building and running OpenGraph collectors and converters.

  OpenHound is built in Python and powered by the [Data Load Tool (DLT)](https://dlthub.com/docs/intro) library so you can follow a consistent workflow to collect, process, and convert data from external sources into BloodHound-compatible graphs. You can run OpenHound as a containerized service or as a standalone CLI application.
</Update>

<Update label="BloodHound" description="New Feature" tags={["OpenGraph"]}>
  {/*BP-2432*/}
  ## OpenGraph Extension Management

  Use the new **OpenGraph Management** page to upload, view, update, and delete OpenGraph extensions so you can manage [structured](/opengraph/extensions/manage) graph support.

  OpenGraph extensions define the schema for your custom graph data, including node and edge types, properties, and relationships. With the new **OpenGraph Management** page, you can manage your extensions in one place and keep custom graph data structured for ingestion.

  This release also exposes OpenGraph Extension Management APIs in the public OpenAPI specification and marks them as experimental so you can evaluate and automate extension workflows with clear API guidance.
</Update>

<Update label="BloodHound" description="New Feature" tags={["OpenGraph"]}>
  {/*BED-7665*/}
  ## OpenGraph Data Validation

  Validate OpenGraph data payloads before uploading them to BloodHound with the new [`chow`](https://github.com/SpecterOps/chow) command-line tool.

  The tool provides actionable context when a payload fails schema checks to help you troubleshoot malformed data faster and reduce ingestion retries.
</Update>

<Update label="BloodHound" description="New Feature" tags={["OpenGraph"]}>
  {/*BED-6738*/}
  {/*BED-7651*/}
  {/*BED-7042*/}
  ## OpenGraph Findings

  <img
    src="/assets/enterprise-edition-pill-tag.svg"
    alt="BloodHound Enterprise logo"
    style={{ width: "25%" }}
  />

  See OpenGraph findings in the **Attack Paths** and **Posture** pages so you can prioritize remediation alongside built-in Active Directory and Entra ID data.

  OpenGraph integration brings custom graph data into the BloodHound Enterprise findings experience, including the metrics and views you use to understand severity, track exposure, and measure remediation progress over time.

  This release also adds APIs to list extension finding schemas and export OpenGraph attack path details so you can power UI rendering and reporting workflows with extension-specific finding definitions and path data.
</Update>

<Update label="BloodHound" description="New Feature" tags={["Administration"]}>
  {/*BP-2428*/}
  ## Environment Targeted Access Control (ETAC)

  <img
    src="/assets/enterprise-edition-pill-tag.svg"
    alt="BloodHound Enterprise logo"
    style={{ width: "25%" }}
  />

  Configure access control policies in BloodHound Enterprise with Environment Targeted Access Control (ETAC), a new premium add-on that dynamically limits environment access for **User** and **Read-only** roles.

  ETAC allows you to scope user access by environment, restricting which environments users can explore and analyze. This is especially useful in large, complex environments where users only need access to a specific subset of environments to perform their work.
</Update>

<Update label="SharpHound" description="New Feature" tags={["Data Collection"]}>
  {/*BP-2335*/}
  ## SharpHound Integrated Windows Authentication

  <img
    src="/assets/enterprise-edition-pill-tag.svg"
    alt="BloodHound Enterprise logo"
    style={{ width: "25%" }}
  />

  Authenticate SharpHound Enterprise with Active Directory Federation Services (ADFS) using Integrated Windows Authentication (IWA).

  This release adds an [IWA-based authentication](/install-data-collector/install-sharphound/configure-adfs-iwa) path for SharpHound Enterprise deployments that rely on ADFS for Windows-based authentication workflows. With this new feature, SharpHound can seamlessly authenticate with ADFS using IWA, providing a more streamlined and secure authentication experience for users in environments that rely on ADFS for identity management.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Data Collection"]}>
  {/*BED-7717*/}
  {/*BED-7710*/}
  ## Azure Hybrid Post-Processing and Data Hygiene

  Improve Azure post-processing and reduce unnecessary graph churn during hybrid data handling.

  This release optimizes role-assignment processing and stops creating placeholder Active Directory (AD) objects during Azure ingest and hybrid post-processing so relationships are only created when both Azure and AD context is available.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Administration"]}>
  {/*BED-5987*/}
  ## HasSession Edge Deletion

  Refresh time-sensitive session data without clearing the entire graph by deleting only **HasSession** edges from the database.

  This enhancement adds a dedicated **HasSession** checkbox to the **Database Management** page so you can remove stale session relationships before recollecting updated session data.

  <Frame>
    <img
      src="/images/release_notes/9_0_0/has-session-edge-deletion.png"
      alt="Screenshot of the HasSession edge deletion checkbox on the Database Management page"
    />
  </Frame>
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Explore"]}>
  {/*BED-7524*/}
  {/*BED-7523*/}
  {/*BED-7539*/}
  ## Graph Readability Improvements

  Explore graph readability with improved node labels, clearer directional arrows, and more intuitive selection behavior:

  - Node labels now display below nodes and include a second line identifying the source platform and type (for example, _Active Directory | Group_). Long labels are truncated with an ellipsis and show the full text on selection.
  
  - Edges now feature higher contrast directional arrows that connect directly to nodes instead of node labels, making it easier to follow relationship paths at default zoom levels.
  
  - The Entity panel now closes when clicking on the graph background to clear selection, giving you more space to explore the graph without manually closing the panel.

    <Frame>
      <img
        src="/images/release_notes/9_0_0/graph-readability.png"
        alt="Screenshot of graph readability improvements, including updated node labels and directional arrows"
      />
    </Frame>
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Explore"]}>
  {/*BED-7032*/}
  ## Cypher Query Timeout Controls

  Take control of long-running Cypher queries with a new option to disable the default query timeout.

  For queries that may require more time to execute, you can now [disable](/analyze-data/explore/cypher-search#disable-query-timeout) the default query timeout on a per-query basis. This gives you more control over long-running queries and allows you to retrieve results that may take longer to compute without being prematurely terminated by the timeout.

  <Frame>
    <img
      src="/images/release_notes/9_0_0/disable-query-timeout.png"
      alt="Screenshot of the Cypher query timeout bypass option in the query editor"
      style={{ width:"100%" }}
    />
  </Frame>
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Explore"]}>
  {/*BED-7002*/}
  {/*BED-7000*/}
  ## Table View Sorting and Column Organization

  Work through table data faster with clearer sorting affordances and better control over column layout.

  This release improves sort discoverability and adds stronger column organization workflows for table-heavy analysis tasks.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Explore"]}>
  {/*BED-7609*/}
  ## Improved Graph Export Filenames

  Export graph data with more useful filenames to avoid repeated `bh-graph.json` collisions in your downloads.

  This enhancement improves filename behavior so exported graph files are easier to identify and manage across repeated exports.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["OpenGraph"]}>
  {/*BED-7693*/}
  ## OpenGraph Environment ID Property Standardization

  Standardize OpenGraph payload and node property handling on `environmentid` to align with BloodHound naming conventions.

  This update migrates legacy `environment_id` usage, updates references, and improves consistency for ingestion, indexing, and query behavior.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Accessibility"]}>
  {/*BED-5843*/}
  {/*BED-5690*/}
  ## Accessibility Labels and Non-Text Content

  Improve assistive technology support with clearer component naming and better non-text content labeling across the interface.

  This accessibility work strengthens screen reader compatibility by improving role, state, and value semantics and adding or refining labels for visual UI elements.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Administration"]}>
  {/*BED-7694*/}
  ## Navigation Bar Modernization

  Use a more predictable navigation experience with improved expand and collapse behavior in the primary sidebar.

  The updated navbar defaults to expanded for new users, supports persistent collapse state, and improves discoverability of menu items and submenus.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Administration"]}>
  {/*BED-6392*/}
  ## Citrix RDP Support Default for New Environments

  Start new environments with **Citrix RDP Support** enabled by default to reduce setup friction where Citrix patterns are expected.

  This change updates default environment configuration behavior for new tenants without modifying existing environment settings.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Zone Builder"]}>
  {/*BED-7356*/}
  ## Privilege Zone Certification Selection Behavior

  Certify principals in Privilege Zones with clearer list selection behavior that stays aligned with the object details panel.

  This update improves checkbox and row interaction handling so selected-object context remains easier to follow during certification workflows.
</Update>

<Update label="BloodHound" description="Enhancement" tags={["Posture"]}>
  {/*BED-7595*/}
  {/*BED-7594*/}
  ## Posture Metric Precision and Custom Range Controls

  <img
    src="/assets/enterprise-edition-pill-tag.svg"
    alt="BloodHound Enterprise logo"
    style={{ width: "25%" }}
  />

  Analyze posture trends with more precise metric display and more reliable custom time-range behavior.

  This release expands how many digits are shown before rounding for key posture metrics and improves custom-range validation and chart alignment to prevent confusing future-time selections and inconsistent trend rendering.
</Update>

<Update label="BloodHound" tags={["Fixed Issues"]}>
  ## API and Auth

  - {/*BED-7652*/} Resolved an issue where fuzzy search on `GET /api/v2/clients` did not consistently apply across `name`, `hostname`, and `configured_user` fields.
  - {/*BED-7411*/} Expanded auditor access to key collection and administration read endpoints, including completed jobs, events, file uploads, and bloodhound-users, introduced a new CollectionReadJobs permission, and removed an unused application configuration management permission.
  - {/*BED-7104*/} Strengthened authorization checks for the `/api/v2/tokens` endpoint so low-privileged users cannot enumerate token metadata for other users through filter manipulation.
  - {/*BED-7628*/} Resolved an issue where the `/api/v2/graphs/edge-composition` endpoint could return duplicate edges in ESC edge composition results.
  - {/*BED-7731*/} Resolved an issue where the **API Explorer** did not correctly validate operator-prefixed values to query parameters, such as `eq` and `neq`.

  ## Cypher and Explore

  - {/*BED-7860*/} Resolved an issue where pressing the space bar in the Cypher query editor on the **Explore** page caused unexpected behavior.
  - {/*BED-7038*/} Resolved an issue where the Cypher autocomplete dropdown on the **Explore** page rendered in the wrong position and may not be visible.
  - {/*BED-6500*/} Resolved an issue where several prebuilt "Shortest paths..." **Saved Queries** queries could time out and fail to return results.
  - {/*BED-7469*/} Resolved an issue where Cypher queries on PostgreSQL failed with the error `ERROR:  invalid reference to FROM-clause entry for table "s0" (SQLSTATE 42P01)`.
  - {/*BED-7054*/} Resolved an issue where `OPTIONAL MATCH` clauses could be translated without a valid prior query frame, causing some Cypher queries to return incorrect results.

  ## OpenGraph

  - {/*BED-7612*/} Resolved an issue where [type filtering](/analyze-data/explore/search#filter-by-node-type) for OpenGraph nodes was ignored in search.
  - {/*BED-7747*/} Resolved an issue causing excessive memory use during ingestion of large OpenGraph data payloads.

  ## Privilege Zones and Posture

  - {/*BED-7716*/} Resolved an issue where long Privilege Zone rule names without spaces could overflow and stretch layout containers.
  - {/*BED-7755*/} Resolved an issue preventing **Affected Objects** accordions for GPO objects from expanding on the **Zone Builder** page.
  - {/*BED-7757*/} Resolved an issue where Large Default Group findings could appear incorrectly under the Tier Zero filter on the **Posture** page.
</Update>

<Update label="AzureHound" tags={["Fixed Issues"]}>
  {/*BED-7634*/} Resolved an issue where AzureHound filtered `AZResourceGroupUserAccessAdmin` results by the **Owner** role instead of the **User Access Administrator** role, causing incorrect `AZUserAccessAdministrator` edges on `AZResourceGroup` nodes.
</Update>