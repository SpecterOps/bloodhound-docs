If {source} is connected to other BloodHound-supported data sources in your environment, such as {otherSources}, make sure the corresponding schema is installed too. In BloodHound Enterprise, some built-in schemas may already be present. Upload any companion schemas that are not already installed. Doing so ensures those cross-platform relationships are modeled correctly in BloodHound.
