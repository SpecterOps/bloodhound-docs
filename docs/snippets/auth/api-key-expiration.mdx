<Note>
  If a BloodHound Enterprise administrator has [configured API key expiration](/manage-bloodhound/auth/api-key-expiration), plan to regenerate keys and update the integration configuration before the key expires to prevent disruptions.
</Note>