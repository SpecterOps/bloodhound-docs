---
title: Overview
description: Discover how Privilege Zones help you organize and segment your environment based on sensitivity and access level.
---

import ContactSales from '/snippets/privilege-zones/contact-sales.mdx';

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE" />

Privilege Zones help you organize objects in your environment into logical groups based on their privilege and risk levels. Using Privilege Zones, you can monitor and maintain the security posture of tiered isolation models.

<Note>The Privilege Zones feature is available under early access.</Note>

## Key concepts

The **Zone Builder** page provides tools for configuring and managing your privilege zones. Once configured, BloodHound Enterprise analyzes your zones and displays findings in the **Attack Paths** and **Posture** pages.

Review these concepts before you configure Privilege Zones.

| Concept           | Description |
| ----------------- | ----------- |
| **Zone**          | A group of objects that represents a hierarchy of control across identity providers and services, based on access level. |
| **Label**         | A flexible way to categorize objects for searching and filtering. |
| **Certification** | <Badge shape="rounded" size="sm" stroke color="purple">Enterprise Edition</Badge> An optional review step that pauses automatic inclusion of newly matched objects in a zone until you certify them. |
| **History**       | An audit log of changes made to zones, labels, and related rules. |

Zones organize objects into a strict hierarchy. BloodHound analyzes how object privileges are assigned and where they can be escalated across your environment.

By default, BloodHound includes a **Tier Zero** zone that represents a set of objects with full control over an environment *and* any objects with control over those objects. See [Tier Zero: Members and Modification](/get-started/security-boundaries/tier-zero-members) to learn more.

<ContactSales/>

If BloodHound Enterprise detects an object in a lower-privileged zone controlling an object in a higher-privileged zone, it identifies it as a finding in the **Attack Paths** and **[Posture](/analyze-data/posture-page)** pages. For example, if a Tier One user can control a Tier Zero server, BloodHound flags it as a violation of the privilege model.

This analysis helps you identify and remediate privilege escalation paths and misconfigurations that violate your security model.

## Object membership

Before working with Privilege Zones, it's important to understand how BloodHound assigns objects as members of zones and labels:

- **Object membership requires rule matching**: Objects are only assigned to a zone or label if they match at least one of the zone's or label's rules
- **Zone membership is exclusive to the highest-priority zone**: If multiple zone rules match an object, BloodHound assigns it to the highest-priority zone only
- **Objects can have multiple labels**: Unlike zones, an object can match multiple label rules simultaneously

## How changes take effect

Most changes to Privilege Zones require an analysis run before they take effect. Understanding when changes become active helps you plan configuration updates and validate that your security model and remediation efforts are working as expected.

### Workflow

The following steps represent the general workflow for making changes to zones and labels and when those changes take effect:

<Steps>
	<Step title="Changes are made">
        You make changes to zones, labels, and rules on the Zone Builder page.
    </Step>
    <Step title="Changes are saved">
		Configuration changes are saved, but most changes are not visible until analysis runs.
	</Step>
	<Step title="Analysis is triggered">
		Changes that require analysis automatically queue an analysis run (see table below).
	</Step>
	<Step title="Analysis runs">
		BloodHound re-evaluates all objects against the updated configuration.
	</Step>
	<Step title="Changes become visible">
		Object membership, metrics, and findings reflect the new configuration.

        <Warning>Analysis may take several minutes to complete depending on the size of your environment. Until analysis finishes, the Zone Builder **Details View** and related metrics will not reflect your latest changes.</Warning>
	</Step>
</Steps>

### Triggers

Not all actions trigger an immediate analysis run or require waiting for analysis to see changes. Use the following table to understand which actions trigger analysis and when changes become visible:

| Action | Triggers Analysis | Changes Visible |
| ------ | :---------------: | --------------- |
| Create a zone | <Icon icon="square-xmark" iconType="solid" color="#ef4444" /> | Immediate (but zone will be empty until rules are added) |
| Update a zone (e.g., reorder, rename, enable/disable analysis) | <Icon icon="square-check" iconType="solid" color="#22c55e" /> | After analysis completes |
| Delete a zone | <Icon icon="square-check" iconType="solid" color="#22c55e" /> | After analysis completes |
| Create or update a label | <Icon icon="square-xmark" iconType="solid" color="#ef4444" /> | Immediate |
| Delete a label | <Icon icon="square-xmark" iconType="solid" color="#ef4444" /> | Immediate |
| Create a rule | <Icon icon="square-check" iconType="solid" color="#22c55e" /> | After analysis completes |
| Update a rule (including enable/disable) | <Icon icon="square-check" iconType="solid" color="#22c55e" /> | After analysis completes |
| Delete a rule | <Icon icon="square-check" iconType="solid" color="#22c55e" /> | After analysis completes |

### Validation

After making changes, follow these steps to confirm they've taken effect:

<Steps>
	<Step title="Monitor analysis status">
		Check your [tenant status](/collect-data/enterprise-collection/monitor#tenant-status) to see if analysis is running.
	</Step>
	<Step title="Wait for completion">
		Analysis must finish before changes are reflected.
	</Step>
	<Step title="Review zone membership">
		Navigate to the affected zone and verify that the expected objects appear.
	</Step>
	<Step title="Check findings">
		Review the **Attack Paths** and **Posture** pages for any new findings or changes in existing findings related to the updated zones.
	</Step>
</Steps>

### Common issues

**I created a new zone rule but don't see any objects in the zone.**

- Analysis may still be running. Wait for it to complete before checking zone membership.
- The rule may not match any objects. Review the rule criteria and test with known objects.
- Multiple zone rules may match the same objects. Check if another zone with higher priority is taking precedence for those objects.

**I updated a zone rule but don't see any changes.**

Analysis is required for rule updates to take effect. Check the analysis status and wait for completion.

**I deleted a zone rule and objects are still showing in the zone.**

- Other rules in the same zone may still match those objects. To see which rules are tagging a specific object, use the [object-based](/analyze-data/privilege-zones/rules#method-2%3A-search-by-object-name-or-id) search method.
- Analysis may still be running. Wait for completion to see the final result.
