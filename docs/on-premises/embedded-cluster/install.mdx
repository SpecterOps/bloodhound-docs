---
title: Install and Configure
description: Use this guide to install and configure BloodHound Enterprise on-premises with the embedded cluster deployment option.
---

import { Checklist} from '/snippets/components/checklist.jsx';

<img noZoom src="/assets/enterprise-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise only" />

In the [Deployment Overview](/on-premises/embedded-cluster/overview), we covered the high-level overview of the embedded cluster deployment option for BloodHound Enterprise on-premises. This guide provides step-by-step instructions for installing and configuring BloodHound Enterprise with the embedded cluster installer.

The embedded cluster installer bundles a Kubernetes distribution (k0s) with the BloodHound Enterprise application, so you can deploy everything on a single Linux host without needing to set up Kubernetes separately.

## Pre-install checklist

The following checklist summarizes the install-blocking gates and pre-deployment work that must be completed before running the installer.

<Note>
  For detailed host sizing, collector specifications, supported filesystems and kernel versions, and the complete set of preflight checks the installer enforces, see the full [system requirements](/on-premises/embedded-cluster/system-requirements).
</Note>

<Checklist checklistKey="embedded-cluster-install">
- Provision a dedicated x86-64 Linux host (any distribution with systemd) with root or sudo access, sized per the [BloodHound Enterprise host](/on-premises/embedded-cluster/system-requirements#bloodhound-enterprise-host) specification
- Back the host with SSD storage (P99 write latency ≤10ms) — the installer blocks on spinning disk
- Stand up a [reverse proxy](/on-premises/embedded-cluster/system-requirements#reverse-proxy) with your SSL certificate to terminate HTTPS in front of BloodHound Enterprise
- Create a [DNS A record](/on-premises/embedded-cluster/system-requirements#dns) for the BloodHound Enterprise FQDN that resolves for both users and collectors
- Open inbound TCP `443` (users and collectors) and `30080` (Admin Console, restricted to admins)
- For online installs, allow outbound HTTPS to `replicated.app`, `proxy.replicated.com`, and `registry.replicated.com`; for air-gapped installs, download the installer bundle ahead of time
</Checklist>

## Installation methods

You can install BloodHound Enterprise using either the **UI-guided installer** or the **CLI-based installer**. Choose the method that fits how you want to configure and deploy the application.

| Method | Use this when |
| --- | --- |
| **Web-based installer** | You want a guided, interactive setup. After you run the install command, the installer prints a URL that you open in a browser (from any machine that can reach the host on the Admin Console port) and configures BloodHound Enterprise through the UI. Recommended for first-time installations and one-off deployments. |
| **CLI-based installer** | You want a non-interactive, scriptable install. You provide a pre-built `ConfigValues` file and the installer password as flags, and preflight checks run on the command line instead of in the UI. Recommended for automated or repeatable deployments. |

<Tip>
  Both methods produce the same installed cluster and application. The CLI method requires you to prepare a `ConfigValues` file ahead of time; the web-based installer collects those values interactively.
</Tip>

### Web-based installer

The web-based installer provides an interactive setup experience through a web interface. It guides you through configuration steps such as domain setup, certificate management, and database connectivity.

TODO: Run end-to-end test to validate and finish steps.

<Steps>
  <Step title="Navigate to the installer portal">
    Log in to the [SpecterOps - BloodHound Enterprise Portal](https://enterprise.replicated.com/bloodhound-enterprise).

    <Note>
      If you don't have access, contact your team administrator or your BloodHound Enterprise account team to request an invitation.
    </Note>
  </Step>
  <Step title="Download & install BloodHound Enterprise">
    The BloodHound Enterprise Portal generates the exact download and install commands for your release, including a signed URL, a short-lived bearer token, and your specific version.
    
    <Note>
      Always copy and run the commands from the portal. The snippets below are placeholder examples that show the shape of each command, not runnable commands.
    </Note>

    1. In the **Select Version** field, choose the version of BloodHound Enterprise you want to install.

    1. SSH into the Linux host where you want to install BloodHound Enterprise.

    1. From the portal, copy and run the command to download the installer archive on the Linux host.

       Placeholder example:

       ```bash
       curl -f "<url>" -H "Authorization: Bearer <token>" -o bloodhound-enterprise-<version>.tar.gz
       ```

    1. From the portal, copy and run the command to extract the installer archive on the Linux host.

       Placeholder example:

       ```bash
       tar -xvzf bloodhound-enterprise-<version>.tar.gz
       ```

       <Note>
         The installer archive contains the embedded cluster application and all necessary dependencies, including the k0s Kubernetes distribution. The installer will set up everything for you on a single Linux host.
       </Note>

    1. From the portal, copy and run the install command on the Linux host, providing the path to your license file.

       TODO: Verify license file location during end-to-end test. Based on Replicated v2 docs (and inferred for v3), the license should be present in the extracted archive directory; the portal also exposes a separate license download as a fallback.

       <Note>
         After extraction, your `license.yaml` file is present alongside the installer binary. The portal also exposes a separate license download if you need to re-fetch it.
       </Note>

       Placeholder example:

       ```bash
       sudo ./bloodhound-enterprise install --license license.yaml
       ```

    1. Complete the following installation steps on the Linux host:

       - Copy password
       - Certificate management
       - Firewall settings
       - Test connectivity
  </Step>
  <Step title="Run the setup wizard">
    <Note>
      If you have an existing PostgreSQL database, make sure to have the connection details ready for the installation process. You can also choose to use the bundled PostgreSQL instance that comes with the installer.
    </Note>

    1. Open the URL provided at the end of the installation process on the Linux host to access the setup wizard.

     1. Complete the setup wizard steps in the UI:

        | Step | Notes |
        | --- | --- |
        | **Domain Configuration** | Use a lowercase hostname with no underscores. Kubernetes ingress rules require lowercase hostnames. |
        | **Application Access** | Configure how users reach the BloodHound Enterprise application. |
        | **Certificates** | Choose whether to have the installer generate a TLS certificate for HTTPS access or upload your own. We recommend uploading your own certificates. |
        | **Database Configuration** | Choose between the embedded PostgreSQL database or provide connection details for an external PostgreSQL database. |
        | **Run Preflight Checks** | Review and resolve any blocking checks before continuing the installation. |
  </Step>
  <Step title="Access BloodHound Enterprise">
    Access the BloodHound Enterprise tenant and complete the following next steps:

    - [Create users](/manage-bloodhound/auth/users-and-roles)
    - [Configure collectors](/install-data-collector/overview)
  </Step>
</Steps>

### CLI-based installer

The CLI-based installer runs the install non-interactively from the command line. You provide your configuration values as a file and your Admin Console password as a flag, and the installer runs preflight checks and completes the deployment without opening a browser.

This method is suited to automated, scripted, or repeatable deployments.

TODO: Run end-to-end test to validate flags, ConfigValues schema, and finish steps.

<Steps>
  <Step title="Navigate to the installer portal">
    Log in to the [SpecterOps - BloodHound Enterprise Portal](https://enterprise.replicated.com/bloodhound-enterprise) to retrieve the installer download command and your license file.

    <Note>
      If you don't have access, contact your team administrator or your BloodHound Enterprise account team to request an invitation.
    </Note>
  </Step>
  <Step title="Prepare the ConfigValues file">
    Create a `ConfigValues` YAML file on the Linux host with the configuration values the installer would otherwise collect through the UI (domain configuration, application access, certificates, and database configuration).

    TODO: Document the BloodHound Enterprise `ConfigValues` schema and provide an annotated example.

    <Note>
      If you have an existing PostgreSQL database, include the connection details in the `ConfigValues` file. To use the bundled PostgreSQL instance, set the corresponding database option instead.
    </Note>
  </Step>
  <Step title="Download & install BloodHound Enterprise">
    The BloodHound Enterprise Portal generates the exact download command for your release, including a signed URL and a short-lived bearer token. You construct the install command locally by adding the headless flags to the base install command.

    <Note>
      Copy the download command from the portal — the snippet below is a placeholder example, not a runnable command.
    </Note>

    1. In the **Select Version** field, choose the version of BloodHound Enterprise you want to install.

    1. SSH into the Linux host where you want to install BloodHound Enterprise.

    1. From the portal, copy and run the command to download the installer archive on the Linux host.

       Placeholder example:

       ```bash
       curl -f "<url>" -H "Authorization: Bearer <token>" -o bloodhound-enterprise-<version>.tar.gz
       ```

    1. Extract the installer archive on the Linux host.

       ```bash
       tar -xvzf bloodhound-enterprise-<version>.tar.gz
       ```

    1. Run the install command on the Linux host with the `--headless`, `--config-values`, and `--installer-password` flags, providing the paths to your license file and ConfigValues file.

       TODO: Verify license file location during end-to-end test. Based on Replicated v2 docs (and inferred for v3), the license should be present in the extracted archive directory; the portal also exposes a separate license download as a fallback.

       <Note>
         After extraction, your `license.yaml` file is present alongside the installer binary. The portal also exposes a separate license download if you need to re-fetch it.
       </Note>

       ```bash
       sudo ./bloodhound-enterprise install \
         --license license.yaml \
         --headless \
         --config-values config-values.yaml \
         --installer-password <password>
       ```

       <Note>
         Host preflight checks run automatically on the command line. Resolve any blocking failures and re-run the install command before continuing.
       </Note>
  </Step>
  <Step title="Access BloodHound Enterprise">
    Access the BloodHound Enterprise tenant and complete the following next steps:

    - [Create users](/manage-bloodhound/auth/users-and-roles)
    - [Configure collectors](/install-data-collector/overview)
  </Step>
</Steps>

#### Ignore preflight checks

In non-production environments where you need to proceed past failing preflight checks (for example, an automated test pipeline running on undersized hardware), you can skip preflight enforcement at install time.

<Warning>
  Skipping host preflight checks is not recommended for production installations. The host preflights guard against conditions — undersized hardware, slow disks, incompatible kernels, missing filesystem options — that cause BloodHound Enterprise to fail or perform poorly under load.
</Warning>

Pass the following flags to the install command:

| Flag | Effect |
| --- | --- |
| `--ignore-host-preflights` | Skips Embedded Cluster host preflight checks (CPU, RAM, disk latency, kernel, filesystem, and so on). |
| `--ignore-app-preflights` | Skips application-level preflight checks defined by BloodHound Enterprise. |
| `--yes` | Auto-confirms the interactive prompt that `--ignore-host-preflights` would otherwise emit. Required for unattended installs. |

Example:

```bash
sudo ./bloodhound-enterprise install \
  --license license.yaml \
  --headless \
  --config-values config-values.yaml \
  --installer-password <password> \
  --ignore-host-preflights \
  --ignore-app-preflights \
  --yes
```

## Access the cluster

When troubleshooting, it can be useful to list the cluster and view logs using the `kubectl` command line tool.

TODO: Need to clean this section up and verify the commands against the latest installer.

<Steps>
  <Step title="SSH into a controller node">
    SSH into a controller node on the Linux host where the embedded cluster is running.
  </Step>
  <Step title="Load kubectl shell">
    Run the following command to load the `kubectl` shell environment where `APP_SLUG` is the unique slug for the application.

     ```bash
     sudo ./APP_SLUG shell
     ```

     <Note>
       The appropriate `kubeconfig` is exported, and the location of useful binaries like `kubectl` and Replicated's preflight and support-bundle plugins is added to `PATH`.
     </Note>
  </Step>
  <Step title="Run common troubleshooting commands">
    Use the available binaries as needed. See the following tabs for common cluster inspection commands:

    <Tabs>
      <Tab title="Processes">
      List all running processes in the cluster:

      ```bash
      kubectl get po -A
      ```

      </Tab>
      <Tab title="Services">
      List all services in the cluster:

      ```bash
      kubectl get services -A
      ```

      </Tab>
      <Tab title="Logs">
      View BloodHound Enterprise logs:

      ```bash
      kubectl logs -n [NAMESPACE] [POD_NAME]
      ```
      </Tab>
    </Tabs>
  </Step>
  <Step title="Exit the kubectl shell">
    When finished, type `exit` or press `Ctrl + D` to exit the shell.
  </Step>
</Steps>

### Filesystem layout

Embedded clusters are built on [k0s](https://k0sproject.io/). All cluster data lives under a single root:

<Tree>
  <Tree.Folder name="/var/lib/embedded-cluster/ <- 40 GB minimum required here" defaultOpen>
    <Tree.File name="bin/ <- EC and k0s binaries" />
    <Tree.Folder name="k0s/ <- k0s data directory" defaultOpen>
      <Tree.Folder name="pki/" defaultOpen>
        <Tree.File name="admin.conf <- cluster kubeconfig" />
      </Tree.Folder>
      <Tree.File name="kubelet/ <- kubelet state and pod volumes" />
      <Tree.File name="containerd/ <- container images and overlay snapshots" />
      <Tree.File name="etcd/ <- etcd data (internal etcd)" />
    </Tree.Folder>
    <Tree.File name="registry/ <- in-cluster registry (airgap installs only)" />
    <Tree.File name="charts/ <- Helm chart cache" />
  </Tree.Folder>
</Tree>

### Relevant paths

The following paths are relevant for troubleshooting the embedded cluster installation:

| Path | Contents |
| --- | --- |
| `/etc/embedded-cluster/` | EC configuration |
| `/var/log/embedded-cluster-install.log` | Installation log |
| `/run/k0s/` | k0s runtime sockets and PID files |
| `/etc/k0s/k0s.yaml` | k0s cluster config (generated, do not edit directly) |  
