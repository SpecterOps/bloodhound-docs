---
hidden: true
title: 'AWS_EKSNodeGroup'
description: 'An EKS managed node group — a pool of EC2 worker instances backing a cluster, sharing a node IAM role, AMI type, capacity type, and scaling configuration.'
icon: '/images/extensions/aws/aws_eksnodegroup.png'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

An EKS managed node group — a pool of EC2 worker instances backing a cluster, sharing a node IAM role, AMI type, capacity type, and scaling configuration.