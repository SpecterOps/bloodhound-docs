---
hidden: true
title: 'AWS_CanRunInstances'
description: 'Principal can call ec2:RunInstances (account-scoped) to launch EC2 instances. Wave-0 primitive that, combined with iam:PassRole, feeds the pass_role_via_ec2 composite to run code under a passed instance-profile role.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: false

Principal can call ec2:RunInstances (account-scoped) to launch EC2 instances. Wave-0 primitive that, combined with iam:PassRole, feeds the pass_role_via_ec2 composite to run code under a passed instance-profile role.