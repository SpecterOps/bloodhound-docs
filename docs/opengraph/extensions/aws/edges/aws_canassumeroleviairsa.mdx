---
hidden: true
title: 'AWS_CanAssumeRoleViaIRSA'
description: 'Data-derived edge from an EKS cluster to an IAM role whose trust policy references the cluster''s OIDC provider (IAM Roles for Service Accounts). Traversable because a Kubernetes admin can run a pod using the annotated service account, and the IRSA webhook injects the role''s web-identity credentials into the pod.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Data-derived edge from an EKS cluster to an IAM role whose trust policy references the cluster's OIDC provider (IAM Roles for Service Accounts). Traversable because a Kubernetes admin can run a pod using the annotated service account, and the IRSA webhook injects the role's web-identity credentials into the pod.