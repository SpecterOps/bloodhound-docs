---
hidden: true
title: 'AWS_NodeGroupHasRole'
description: 'Structural edge from an EKS node group to its node IAM role (from the node group''s nodeRole). Traversable because scheduling a pod on the node group exposes that role''s credentials via the EC2 Instance Metadata Service at 169.254.169.254.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Structural edge from an EKS node group to its node IAM role (from the node group's nodeRole). Traversable because scheduling a pod on the node group exposes that role's credentials via the EC2 Instance Metadata Service at 169.254.169.254.