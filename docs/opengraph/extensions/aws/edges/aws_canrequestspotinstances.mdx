---
hidden: true
title: 'AWS_CanRequestSpotInstances'
description: 'Principal can call ec2:RequestSpotInstances (account-scoped); the request''s LaunchSpecification carries an IamInstanceProfile and UserData, granting the same launch-with-a-role plus code-execution primitive as ec2:RunInstances. Wave-0 primitive feeding the pass_role_via_ec2_spot_instances composite.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: false

Principal can call ec2:RequestSpotInstances (account-scoped); the request's LaunchSpecification carries an IamInstanceProfile and UserData, granting the same launch-with-a-role plus code-execution primitive as ec2:RunInstances. Wave-0 primitive feeding the pass_role_via_ec2_spot_instances composite.