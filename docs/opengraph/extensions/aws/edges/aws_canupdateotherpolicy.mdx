---
hidden: true
title: 'AWS_CanUpdateOtherPolicy'
description: 'Principal can both control another principal (CanAssumeRole for a role; CanCreateAccessKey/CanCreateLoginProfile/CanUpdateLoginProfile for a user) and re-permission it (put/attach policy, or CreatePolicyVersion on a policy attached to it), letting it grant the dest admin and act as it. Targets the account; gated by the dest having no capping permissions boundary. The methods property records, per dest principal, the control and re-permission techniques.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Principal can both control another principal (CanAssumeRole for a role; CanCreateAccessKey/CanCreateLoginProfile/CanUpdateLoginProfile for a user) and re-permission it (put/attach policy, or CreatePolicyVersion on a policy attached to it), letting it grant the dest admin and act as it. Targets the account; gated by the dest having no capping permissions boundary. The methods property records, per dest principal, the control and re-permission techniques.