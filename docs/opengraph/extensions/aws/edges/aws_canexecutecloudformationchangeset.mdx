---
hidden: true
title: 'AWS_CanExecuteCloudFormationChangeSet'
description: 'Principal can call both cloudformation:CreateChangeSet and cloudformation:ExecuteChangeSet to apply a rewritten template to an existing stack, which CloudFormation executes as the stack''s execution role (no iam:PassRole needed); the compromise continues to that role via AWS_RunsAs'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Principal can call both cloudformation:CreateChangeSet and cloudformation:ExecuteChangeSet to apply a rewritten template to an existing stack, which CloudFormation executes as the stack's execution role (no iam:PassRole needed); the compromise continues to that role via [AWS_RunsAs](/opengraph/extensions/aws/edges/aws_runsas).