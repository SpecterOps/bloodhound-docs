---
hidden: true
title: 'AWS_CanCreateAndAssumeAdminRole'
description: 'Principal can create a new IAM role (iam:CreateRole), attach an admin policy to it (iam:AttachRolePolicy/PutRolePolicy on role/*), and assume it (sts:AssumeRole) — a self-trusting admin role they assume directly, gaining account admin'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Principal can create a new IAM role (iam:CreateRole), attach an admin policy to it (iam:AttachRolePolicy/PutRolePolicy on role/*), and assume it (sts:AssumeRole) — a self-trusting admin role they assume directly, gaining account admin.