---
hidden: true
title: 'AWS_CanCreateAndPassAdminRole'
description: 'Principal can create a new IAM role, attach an admin policy to it, and pass it to one or more AWS services (EC2/Lambda/CloudFormation/EKS) to execute as it — account takeover. The services/methods properties record which services are usable'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Principal can create a new IAM role, attach an admin policy to it, and pass it to one or more AWS services (EC2/Lambda/CloudFormation/EKS) to execute as it — account takeover. The services/methods properties record which services are usable.