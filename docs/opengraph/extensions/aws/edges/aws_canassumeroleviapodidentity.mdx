---
hidden: true
title: 'AWS_CanAssumeRoleViaPodIdentity'
description: 'Data-derived edge from an EKS cluster to an IAM role mapped via an EKS Pod Identity association. Traversable because a Kubernetes admin can run a pod using the mapped service account, and the EKS Pod Identity Agent injects the role''s credentials automatically.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: true

Data-derived edge from an EKS cluster to an IAM role mapped via an EKS Pod Identity association. Traversable because a Kubernetes admin can run a pod using the mapped service account, and the EKS Pod Identity Agent injects the role's credentials automatically.