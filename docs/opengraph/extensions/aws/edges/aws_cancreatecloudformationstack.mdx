---
hidden: true
title: 'AWS_CanCreateCloudFormationStack'
description: 'Principal can call cloudformation:CreateStack (account-scoped) to provision a stack. Wave-0 primitive that, combined with iam:PassRole, feeds the pass_role_via_cloudformation composite to act as a passed stack role.'
---

<img noZoom src="/assets/enterprise-AND-community-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise and CE"/>

## Edge Schema

Traversable: false

Principal can call cloudformation:CreateStack (account-scoped) to provision a stack. Wave-0 primitive that, combined with iam:PassRole, feeds the pass_role_via_cloudformation composite to act as a passed stack role.