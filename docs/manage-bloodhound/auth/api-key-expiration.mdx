---
title: Configure API Key Expiration
description: Learn how to configure API key expiration in BloodHound Enterprise to enhance security and meet compliance requirements.
---

<img noZoom src="/assets/enterprise-edition-pill-tag.svg" alt="Applies to BloodHound Enterprise only" />

API key expiration controls how long API tokens remain valid. Use API key expiration to reduce long-lived credentials and support internal security and compliance requirements.

API key expiration applies to the following:

| API token type                                                                 | Description                                                            |
|------------------------------------------------------------------------------|------------------------------------------------------------------------|
| [Personal API tokens](/integrations/bloodhound-api/working-with-api#personal-api-tokens)         | Created by individual users for API access.                              |
| [Non-personal API tokens](/integrations/bloodhound-api/working-with-api#non-personal-api-tokens) | Created for integrations such as Splunk, ServiceNow, and Cortex XSOAR.   |
| [Collector client API tokens](/collect-data/enterprise-collection/create-collector)               | Created for SharpHound Enterprise and AzureHound Enterprise data collectors. |

## Before you begin

Before configuring API key expiration, consider the following:

- API key expiration is not enabled by default and must be turned on by a user with the **Administrator** role in BloodHound Enterprise.
- Only users with the **Administrator** role can configure API key expiration.
- After enabling API key expiration, all existing API keys will be set to expire based on the configured validity period.
- BloodHound Enterprise does not automatically rotate API keys.
- Organizations should plan manual key rotation for all integrations and collector clients before changing this setting.

## Configuration

The following table summarizes how API key expiration settings and actions affect API tokens:

| Setting or action | Behavior |
| --- | --- |
| Feature enabled | New tokens expire according to the default of custom rotation period. Existing tokens **are not** affected. |
| Default rotation period | New tokens expire after 90 days by default. |
| Custom rotation period | New tokens expire after the configured number of days (between 1 and 365). |
| Rotation period changes | Each token keeps the earlier of its current expiration and now + configured days. |
| Feature disabled | Expiration enforcement stops immediately, expiration dates are cleared, and tokens no longer expire. |

### Enable expiration

To enable and configure API key expiration:

<Steps>
  <Step title="Navigate to the configuration">
    1. Log in to BloodHound Enterprise as a user with the **Administrator** role.

    1. In the left menu, click **Administration** > **BloodHound Configuration**.
  </Step>
  <Step title="Enable API key expiration">
    Toggle the setting for API key expiration.

        <Frame>
          <img src="/images/admin/enable-api-key-expiration.png" alt="API key expiration setting in BloodHound Enterprise admin settings"/>
        </Frame>
  </Step>
  <Step title="Set the validity period for API keys">
    Enter the number of days (between 1 and 365) for API keys to remain valid.
    
    This value will apply to all new keys going forward. Keys created before enabling the feature are not affected.
  </Step>
  <Step title="Save changes and confirm modal">
    Click **Save Settings** and confirm the modal that appears, which explains how existing keys will be affected by this change.

        <Frame>
          <img src="/images/admin/api-key-expiration-modal.png" alt="Modal warning about API key expiration changes"/>
        </Frame>
  </Step>
</Steps>

### Modify rotation period

To modify the rotation period for API keys after enabling expiration:

<Steps>
  <Step title="step 1">
    d
  </Step>
  <Step title="step 2">
    d
  </Step>
</Steps>

### Disable expiration

To disable API key expiration:

<Steps>
  <Step title="step 1">
    d
  </Step>
  <Step title="step 2">
    d
  </Step>
</Steps>

## Rotate keys

After API key expiration is enabled, Administrators must manually rotate keys before they expire to maintain uninterrupted access to the BloodHound Enterprise API. This involves regenerating API tokens and updating any integrations or collector clients that use the expiring keys with the new credentials.

Administrators can monitor expiration dates in the **Manage Clients** page for collector clients and in the **API Key Management** page for personal and non-personal API tokens. Both pages display expiration dates, allowing administrators to proactively manage key rotation.

To rotate an API key for personal or non-personal API tokens:

<Steps>
  <Step title="step 1">
    d
  </Step>
  <Step title="step 2">
    d
  </Step>
</Steps>

To rotate an API key for a collector client:

<Steps>
  <Step title="step 1">
    d
  </Step>
  <Step title="step 2">
    d
  </Step>
</Steps>
